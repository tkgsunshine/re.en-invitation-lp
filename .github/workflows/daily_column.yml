name: Daily Column Automation Pipeline (Re:en)

on:
  schedule:
    # Runs twice daily at 10:00 AM JST (01:00 UTC) and 7:00 PM (19:00) JST (10:00 UTC)
    - cron: '0 1,10 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publish-column:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Run Column Publishing Pipeline
        run: |
          python3 run_matching_column_pipeline.py

      - name: Push changes to repository
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Auto-publish scheduled column with full regulation compliance [skip ci]"
          branch: main
